# Bunnystack — Phase 2: Production-Ready Template

> **Goal:** Transform the scaffold into a fully-functional, deployable starter
> template that a new project can clone and start building on immediately.

**Current state:** Scaffold committed locally (37 files, 1097 lines).
All 7 packages pass type-checking. No DB, no auth routes, no deployment config,
no CI/CD, not pushed to GitHub.

---

## Phase 2 Overview

### 1. GitHub Publishing

Push to a public repo so it becomes the canonical `bun create` / clone target
for all future projects.

**Tasks:**
- Create `bunnystack/bunnystack` on GitHub via `gh repo create`
- Push `main` branch
- Add a "Use this template" badge / note in README

**Files:**
- Create: (none — GitHub CLI creates the remote)
- Modify: `README.md` (add clone URL + badge)

---

### 2. Local Postgres + DB Migrations

Get the database layer live so auth and schema actually work out of the box.

**Tasks:**
- Add `docker-compose.yml` at root with a Postgres 17 service
- Create `.env.example` with `DATABASE_URL` and `PORT` defaults
- Generate initial Drizzle migration from `schema.ts`
- Add a `db:up` script in root `package.json` (docker compose up -d db)
- Verify migration runs and schema is applied

**Files:**
- Create: `docker-compose.yml`, `.env.example`
- Modify: `package.json` (add `db:up`, `db:migrate`, `db:generate` scripts),
          `.gitignore` (add `.env` if missing)

**Validation:**
- `docker compose up -d db` starts Postgres
- `bun run --filter @bunnystack/db generate && bun run --filter @bunnystack/db migrate` applies schema
- `drizzle-kit studio` shows the `users` table

---

### 3. Better-Auth Integration with ElysiaJS

The auth package exists but is not wired into the API server. Better-Auth
needs ElysiaJS plugins/routes mounted to serve auth endpoints.

**Tasks:**
- Install `@better-auth/elysia` in `apps/api`
- Mount Better-Auth on `/api/auth` in the Elysia server
- Set up session validation middleware for protected routes
- Expand `schema.ts` with Better-Auth's required tables (sessions, accounts,
  verifications) — Better-Auth can auto-generate these via Drizzle adapter

**Files:**
- Create: (none — in-place changes)
- Modify: `apps/api/src/index.ts` (mount auth),
          `apps/api/package.json` (add `@better-auth/elysia`),
          `packages/auth/src/auth.ts` (add Drizzle adapter config)
          `packages/db/src/schema.ts` (add auth tables or use better-auth schema)

**Validation:**
- `bun run check-types` passes
- `bun run dev` starts API
- `curl localhost:3000/api/auth/sign-up` returns a response

---

### 4. Environment Variable Management

Centralize all env vars so a new project just copies `.env.example` and fills
in values.

**Tasks:**
- Populate `.env.example` with all expected vars:
  - `DATABASE_URL`
  - `PORT`
  - `BETTER_AUTH_SECRET`
  - `BETTER_AUTH_URL`
- Add variable validation in `packages/config/src/env.ts` (throw on missing
  production-critical vars, warn on defaults in development)
- Document all env vars in README

**Files:**
- Create: `.env.example`
- Modify: `packages/config/src/env.ts`

**Validation:**
- `bun run check-types` passes
- Starting API with missing `DATABASE_URL` prints a clear error

---

### 5. Fly.io Deployment Config

Add `fly.toml` and a Dockerfile so `fly deploy` works immediately.

**Tasks:**
- Create `Dockerfile` (multi-stage: Bun install → build → dist)
- Create `fly.toml` (single-process app, port 3000, attached Postgres)
- Add fly launch notes to README

**Files:**
- Create: `Dockerfile`, `fly.toml`
- Modify: `README.md`

**Validation:**
- `docker build -t bunnystack .` succeeds
- (Manual) `fly launch --no-deploy` reads the config

---

### 6. CI/CD with GitHub Actions

Basic CI that runs on PRs and pushes to main.

**Tasks:**
- Create `ci.yml` — runs `bun install`, `bun run check-types`, `bun run build`
- Optionally: `deploy.yml` — runs on push to main, deploys to Fly.io

**Files:**
- Create: `.github/workflows/ci.yml`, `.github/workflows/deploy.yml`

**Validation:**
- Pushing to a PR branch triggers CI
- CI shows green check on PR

---

### 7. Web Frontend Polish

The React app is currently a single component with no CSS, no routing, no
API client, no auth pages.

**Tasks:**
- Add `tailwindcss` + `@tailwindcss/vite` to `apps/web`
- Add `react-router` for client-side routing
- Create a basic layout with login/signup pages that call the auth API
- Create an API client helper (`src/lib/api.ts`) using fetch

**Files:**
- Create: `apps/web/src/lib/api.ts`,
          `apps/web/src/pages/Login.tsx`,
          `apps/web/src/pages/SignUp.tsx`,
          `apps/web/src/pages/Dashboard.tsx`,
          `apps/web/src/App.css` (Tailwind entry)
- Modify: `apps/web/package.json` (add deps),
          `apps/web/vite.config.ts` (Tailwind plugin),
          `apps/web/src/main.tsx` (routing),
          `apps/web/src/App.tsx` (router setup)

**Validation:**
- `bun run dev` from root starts both API + web
- Navigating to `localhost:5173` shows a styled page
- Sign-up form posts to `/api/auth/sign-up` successfully (with Postgres running)

---

### 8. Mobile & Desktop Placeholder → Real Scaffolds

Replace empty placeholder files with proper starter code.

**Tasks (Mobile):**
- Add React Native + Expo config
- Add `package.json` scripts that actually do something
- README note: "not yet implemented — generated placeholder"

**Tasks (Desktop):**
- Add Electron + Vite config
- Same README note approach

**Files:**
- Modify: `apps/mobile/package.json`, `apps/mobile/src/index.ts`
- Modify: `apps/desktop/package.json`, `apps/desktop/src/index.ts`

---

### 9. Documentation Update

README should be a proper template manual.

**Tasks:**
- Add "Quick Start" section (clone → `bun install` → `bun run dev`)
- Add "What's in the box" table with links
- Add "Creating a new project" section
- Add "Deployment" section (Fly.io)
- Add "Environment Variables" table
- Add "Project Structure" diagram

**Files:**
- Modify: `README.md`

---

## Execution Order

| Step | Description | Dependencies |
|------|-------------|--------------|
| 1    | GitHub push | None |
| 2    | Docker + DB migrations | None |
| 3    | Auth integration | Step 2 (needs running DB) |
| 4    | Env management | None |
| 5    | Fly.io config | None |
| 6    | CI/CD | Step 1 (needs remote repo) |
| 7    | Web frontend | Step 3 (needs auth API) |
| 8    | Mobile/Desktop scaffolds | None |
| 9    | Documentation | After all steps |

**Recommended batch execution:**
1. Steps 1 + 2 + 4 + 5 + 8 (no dependencies, can be done in parallel via
   subagent delegation)
2. Step 3 (needs DB)
3. Step 6 + 7 (needs GitHub + auth API)
4. Step 9 (needs everything)

---

## Risks & Tradeoffs

| Risk | Mitigation |
|------|------------|
| Better-Auth Drizzle adapter API may differ from v1.2.0 | Pin exact version; test auth flow before committing |
| Postgres required locally | docker-compose handles this; note in README for non-Docker users |
| React Native + Electron add weight to template | Keep as opt-in placeholders; document that they're optional |
| Fly.io free tier limits | Note in README; add `fly deploy` note about paid plan |
| Tailwind v4 API changed from v3 | Pin version and verify config syntax |

---

## Open Questions

1. Should `apps/mobile` and `apps/desktop` stay in the template or be
   documented as "add when needed" and removed from the workspace?
2. Deploy `deploy.yml` — auto-deploy to Fly.io on push to main, or manual?
3. Should there be a `bun create bunnystack` CLI or just a template repo?
4. Want Husky + lint-staged for pre-commit checks?

---

*Plan written: 2026-05-28 05:30 UTC*