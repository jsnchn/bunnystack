/**
 * @bunnystack/desktop — Electron app (placeholder)
 *
 * To activate this app:
 *   1. `bun add -d electron electron-builder`
 *   2. Create `src/main.ts` as the Electron main process entry point
 *   3. Create `src/preload.ts` for context bridge
 *   4. Create `electron-builder.yml` for packaging config
 *   5. Update this `package.json` `"dev"` script to `"electron src/main.ts"`
 *   6. Run `bun run dev` to launch the Electron window
 *
 * This placeholder reserves the workspace name and validates that the
 * monorepo's TypeScript tooling works for the desktop package.
 */
export const description =
  "Electron desktop app — placeholder (not yet activated)";
