{
  "name": "mobile",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'React Native not yet activated. See src/index.ts for setup instructions.'",
    "build": "echo 'TODO: React Native build'",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@bunnystack/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
