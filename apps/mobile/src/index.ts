/**
 * @bunnystack/mobile — React Native app (placeholder)
 *
 * To activate this app:
 *   1. `bun add react react-native`
 *   2. `bun add -d @react-native/metro-config @react-native/babel-preset \
 *        @react-native/typescript-config`
 *   3. Create `metro.config.js` and `babel.config.js`
 *   4. Create `index.js` as the React Native entry point
 *   5. Update this `package.json` `"dev"` script to `"npx react-native start"`
 *   6. Run `bun run dev` to start the Metro bundler
 *
 * This placeholder reserves the workspace name and validates that the
 * monorepo's TypeScript tooling works for the mobile package.
 */
export const description =
  "React Native mobile app — placeholder (not yet activated)";
